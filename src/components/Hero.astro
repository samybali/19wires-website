---
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20"
>
  <!-- Fond animé -->
  <div class="absolute inset-0 -z-10">
    <div
      class="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full bg-blue-700/15 blur-[120px]"
    ></div>
    <div
      class="absolute bottom-0 right-0 w-[400px] h-[400px] rounded-full bg-sky-600/10 blur-[100px]"
    ></div>
    <!-- Grid pattern -->
    <div
      class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:64px_64px]"
    ></div>
  </div>

  <div class="max-w-7xl mx-auto px-6 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-blue-500/30 bg-blue-500/10 text-blue-300 text-sm font-medium mb-8">
      <span class="w-2 h-2 rounded-full bg-blue-400 animate-pulse"></span>
      Agence disponible pour nouveaux projets
    </div>

    <!-- Titre principal -->
    <h1
      class="text-5xl md:text-7xl lg:text-8xl font-black tracking-tight leading-[1.05] mb-6"
    >
      On transforme vos
      <br />
      <span
        class="bg-gradient-to-r from-blue-400 via-sky-400 to-indigo-400 bg-clip-text text-transparent"
      >
        idées en pixels
      </span>
    </h1>

    <!-- Sous-titre -->
    <p class="max-w-2xl mx-auto text-lg md:text-xl text-zinc-400 leading-relaxed mb-10">
      Nous concevons des sites web et applications sur mesure qui convertissent,
      performent et marquent les esprits. Design premium, code propre.
    </p>

    <!-- CTA buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href="/contact"
        class="group inline-flex items-center gap-2 px-8 py-4 rounded-full bg-blue-600 hover:bg-blue-500 text-white font-semibold text-base transition-all duration-200 hover:shadow-2xl hover:shadow-blue-500/30 hover:-translate-y-0.5"
      >
        Démarrer un projet
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-4 h-4 transition-transform group-hover:translate-x-1"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M17 8l4 4m0 0l-4 4m4-4H3"
          ></path>
        </svg>
      </a>
      <a
        href="#services"
        class="inline-flex items-center gap-2 px-8 py-4 rounded-full border border-zinc-700 hover:border-zinc-500 text-zinc-300 hover:text-white font-semibold text-base transition-all duration-200 hover:-translate-y-0.5"
      >
        Voir nos services
      </a>
    </div>

    <!-- Stats -->
    <div
      class="mt-20 grid grid-cols-2 md:grid-cols-4 gap-8 max-w-3xl mx-auto border-t border-zinc-800 pt-12"
    >
      {
        [
          { value: "120+", label: "Projets livrés" },
          { value: "98%", label: "Clients satisfaits" },
          { value: "5 ans", label: "D'expérience" },
          { value: "12", label: "Experts dédiés" },
        ].map((stat) => (
          <div class="text-center">
            <div class="text-3xl md:text-4xl font-black text-white mb-1">
              {stat.value}
            </div>
            <div class="text-sm text-zinc-500">{stat.label}</div>
          </div>
        ))
      }
    </div>
  </div>

  <!-- Scroll indicator -->
  <div
    class="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 text-zinc-600"
  >
    <span class="text-xs uppercase tracking-widest">Scroll</span>
    <div class="w-px h-12 bg-gradient-to-b from-zinc-600 to-transparent"></div>
  </div>
</section>